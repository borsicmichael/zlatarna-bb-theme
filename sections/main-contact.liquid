<div class="page-width">
  <article class="page-content">
    <h1>{{ page.title | escape }}</h1>
    {%- if page.content != blank -%}<div class="rte" style="margin-bottom:32px">{{ page.content }}</div>{%- endif -%}
    {%- form 'contact', id: 'ContactForm' -%}
      {%- if form.posted_successfully? -%}
        <p class="form-message" role="status" style="margin-bottom:16px">{{ section.settings.success }}</p>
      {%- elsif form.errors -%}
        <p class="form-message" role="alert" style="margin-bottom:16px">{{ form.errors | default_errors }}</p>
      {%- endif -%}
      <div style="display:flex;flex-direction:column;gap:16px">
        <div class="field">
          <label class="field__label" for="ContactName">{{ 'newsletter.name' | t }}</label>
          <input class="field__input" id="ContactName" type="text" name="contact[name]" autocomplete="name" value="{{ form.name }}">
        </div>
        <div class="field">
          <label class="field__label" for="ContactEmail">{{ 'newsletter.email' | t }}</label>
          <input class="field__input" id="ContactEmail" type="email" name="contact[email]" autocomplete="email" required value="{{ form.email }}">
        </div>
        <div class="field">
          <label class="field__label" for="ContactPhone">Telefon</label>
          <input class="field__input" id="ContactPhone" type="tel" name="contact[phone]" autocomplete="tel" value="{{ form.phone }}">
        </div>
        <div class="field">
          <label class="field__label" for="ContactBody">Sporočilo</label>
          <textarea class="field__input" id="ContactBody" name="contact[body]">{{ form.body }}</textarea>
        </div>
        <button type="submit" class="btn btn--light">{{ section.settings.button_label | escape }}</button>
      </div>
    {%- endform -%}
  </article>
</div>

{% schema %}
{
  "name": "Kontaktni obrazec",
  "class": "section-main-contact",
  "settings": [
    { "type": "text", "id": "button_label", "label": "Gumb", "default": "Pošlji sporočilo" },
    { "type": "text", "id": "success", "label": "Sporočilo ob uspehu", "default": "Hvala! Odgovorili vam bomo v najkrajšem možnem času." }
  ]
}
{% endschema %}
